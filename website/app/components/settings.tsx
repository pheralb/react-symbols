import ThemeSwitcher from "./themeSwitcher";

const Settings = () => {
  return (
    <nav className="fixed right-3 top-2 z-50 md:top-2.5">
      <ThemeSwitcher />
    </nav>
  );
};

export default Settings;
